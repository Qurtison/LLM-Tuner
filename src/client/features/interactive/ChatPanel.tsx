// Placeholder for Phase 5 slice 3 — Chat streaming and slot polling. Replaced by its conversion agent.
export default function ChatPanel() {
    return (
        <div className="p-6 text-sm text-neutral-400">
            ChatPanel — slice 3 (not converted yet)
        </div>
    );
}
